{
    "api-key": {
        "name": "api-key",
        "type": "api-key",
        "disabled": true
    },
    "custom-token": {
        "name": "custom-token",
        "type": "custom-token",
        "config": {
            "audience": [],
            "signingAlgorithm": "HS256",
            "useJWKURI": false
        },
        "secret_config": {
            "signingKeys": [
                "mello-key"
            ]
        },
        "disabled": false,
        "metadata_fields": [
            {
                "required": false,
                "name": "Push",
                "field_name": "Mellospace"
            }
        ]
    }
}
